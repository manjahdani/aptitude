/** 
 * 
 */
package be.uclouvain.organisation.platform

import be.uclouvain.organisation.platform.ObserverCapacity
import be.uclouvain.organisation.platform.util.OrientedCountingLine
import java.util.HashMap

/** 
 * @author manjah
 * 
 */
capacity CounterObserverCapacity extends ObserverCapacity {

			/** 
		 * The capacity is able to display  
		 * 
		 * 
		 */
	def DisplayPerception(actionID : int) : HashMap<String, OrientedCountingLine> 		// @FIXME : It is not an ObserverCapacity
}
