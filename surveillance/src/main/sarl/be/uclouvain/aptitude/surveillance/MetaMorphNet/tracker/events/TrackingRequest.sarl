package be.uclouvain.aptitude.surveillance.MetaMorphNet.tracker.events

import java.util.UUID

event TrackingRequest {
	
	val missionID : UUID
	var agentID : UUID

	new (mID:UUID, aID : UUID) {
		this.missionID = mID
		this.agentID = aID
	}
}
