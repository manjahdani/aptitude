package be.uclouvain.aptitude.surveillance.algorithm.tracker

import java.util.UUID

event TrackingRequest {
	
	val missionID : UUID
	var agentID : UUID

	new (mID:UUID, aID : UUID) {
		this.missionID = mID
		this.agentID = aID
	}
}
