/** 
 * 
 */
package be.uclouvain.organisation.platform

import io.sarl.core.Logging

/** 
 * @author manjah
 * 
 */
behavior ObserverRole {
	uses Logging, ObserverCapacity

	on LeavePlatform {
		// join(BaseContext.ID, BaseSpace.spaceID.ID)
		leave(WorldID)
	}

}
